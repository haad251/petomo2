<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.ejo.petwalk.dao.ReservationMapper">

<!-- 예약번호 기준 채팅 불러오기 -->
 <select id="selectAllChat"  parameterType="String" resultType="HashMap">
      	SELECT *
		FROM chat_tb
		WHERE res_id=#{res_id}
 </select>

<!-- 소켓전송한 채팅 인서트 -->
<insert id="insertChat" parameterType="ChatVO">
	INSERT INTO chat_tb ( res_id,chat_sender,chat_receiver,chat_content,chat_date)
	VALUES(
 
		#{res_id},
		#{chat_sender},
		#{chat_receiver},
		#{chat_content},
		#{chat_date}
	)
</insert>

<!-- 예약번호로 예약불러오기 -->
 <select id="selectOneRes"  parameterType="String" resultType="ReservationVO">
      	SELECT *
		FROM reservation_tb
		WHERE res_id=#{res_id}
 </select>


</mapper>
